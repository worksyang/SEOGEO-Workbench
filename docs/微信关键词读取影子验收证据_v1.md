# 微信关键词读取影子验收证据 v1

- **执行时间**：`20260716T115149`（Asia/Shanghai）
- **旧隔离参考实例**：`http://127.0.0.1:8774`；新 Hub：`127.0.0.1:8775`
- **冻结基准**：`data/migration/wechat/freeze_20260716T024524+0800/payload`
- **范围护栏**：仅读取 8774；8765 在 preflight、导入前后和最终检查均不可连接，旧源 runner/watchdog 进程不存在，两个 launchd label 均核对为 disabled 且未加载；未调用真实抓取、Aidso 或封面外网；临时 SQLite/asset_store 已清理；未 commit/push。

## 结果摘要
- 导入：**两次均 HTTP 200**；第二次用于幂等重放。
- 22 个 GET + 非法/缺失样本：35 请求；状态相同 35，原始解压后 JSON/正文相同 35，按合同语义相同 35，HTTP 元数据五字段相同 35；容差通过 0 条（仅 keyword-manage /updated_at，<=5 秒）。
- 全部身份读取：312 keyword 请求，payload 相同 312；1361 account 请求，payload 相同 1361。
- 6364 article 身份清单来自冻结 normalized：正文路径 4328，已导入路径资产 4328，缺失路径资产 0；源本来无 content_file_path 2036（不计迁移丢失）；物理去重 blob 4144，聚合 hash `001bfe59739da64691b5ed1f3689d13038da59f041f2e37431c2e3f61fdb8e60`。
- R21：7 组排序/筛选，覆盖每组 page=1、末页、末页+1，共 20 个边界请求；状态全相同=True，payload 全相同=True。
- R21 默认 `time_range=15` 使用旧系统同样的动态 `datetime.now() - 15 days` cutoff；同一冻结数据在核验时刻不同，`total` 可以变化，不代表迁移丢数：2026-07-16 12:01:00 的旧算法复刻为 **1485**，2026-07-16 13:38:49 为 **1482**。两者差异恰为以下 3 条位于时间窗口边界的文章：`art_3dccf2198a33`（`2026-07-01T12:44:00`）、`art_f1ee884bd4d9`（`2026-07-01T13:11:00`）、`art_0b64bd939c33`（`2026-07-01T13:14:00`）。旧系统与 Hub 共同遵循这一动态时间窗口。
- R15：冻结只读 app.db 推导页面期望={'groups': 18, 'visible_keywords': 312}，返回体实测 legacy={'groups': 18, 'visible_keywords': 312}、Hub={'groups': 18, 'visible_keywords': 312}；底层 registry 期望/对账=457/{'source': 457, 'hub': 457}；pass=True。
- compare 模式回显 legacy：status_match=True，payload_match=True，decoded_hash_match=True，五个头全相同=True，逐头={'content-type': True, 'content-encoding': True, 'etag': True, 'cache-control': True, 'vary': True}。

## 差异与处置
- 证据 JSON：`data/migration/wechat/shadow_read/latest.json`；每个差异保留 HTTP 五字段、状态、解压后 hash 与最多 40 个 JSON Pointer 差异。
- 首批核心差异：0 条（详见 JSON；未因失败修改产品代码）。
- R21 首批差异：0 条；身份差异样本见 JSON 的 `identity_results`。
> 若出现差异，建议先按 JSON Pointer 区分投影契约差异、排序/分页差异、HTTP 缓存头差异和正文资产缺失，再回到对应模块契约修复；本次 harness 不自动修产品代码。

## 可重复命令
```bash
cd /Users/works14/Documents/zkcode/260712_SEO-GEO
python3 workbench/tests/test_wechat_shadow_e2e.py --run
```

## 证据文件
- `data/migration/wechat/shadow_read/run_20260716T115149/result.json`
- 临时服务退出后不会保留临时 Hub 数据库与 asset_store；冻结 payload 未写入。
