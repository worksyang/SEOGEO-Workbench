# 微信关键词写副本隔离证据 v1

- **执行日期**：2026-07-16（Asia/Shanghai）
- **冻结基准**：`data/migration/wechat/freeze_20260716T024524+0800/payload`
- **写副本根**：`data/migration/wechat/write_reference/instance`
- **端口**：`127.0.0.1:8775`；启动前检查占用，当前检查为未占用；`8765` 未启动
- **启动方式**：`data/migration/wechat/write_reference/tools/start_write_reference.sh`；长任务/前台验收使用 `start_write_reference_foreground.sh`
- **停止方式**：`data/migration/wechat/write_reference/tools/stop_write_reference.sh`
- **重置方式**：`data/migration/wechat/write_reference/tools/reset_write_reference.sh`
- **回放方式**：`python3 data/migration/wechat/write_reference/tools/replay_write_reference.py`
- **PID**：由 `write_reference.pid` 记录；本次前台验收 PID 为 `10598`，已停止
- **未 commit / 未 push**；未修改 `workbench/`、旧真实源、`freeze_.../payload`、现有 `data/migration/wechat/reference`

## 1. 副本与基线

副本只从最终 freeze payload 复制，未读取旧真实源；APFS `cp -cR` 成功。准备基线记录在 `evidence/baseline.json`：

| 项目 | 基线 |
|---|---:|
| 业务树文件数（排除 `.DS_Store`、日志、SQLite 临时文件） | 63,461 |
| 业务树字节数 | 1,115,458,996 |
| SQLite 完整性 | `ok` |
| SQLite 表/行数 | candidates 352、evidence 392、probes 46、groups 18、registry 457 |
| `monitor-data` 关键词/账号 | 312 / 1,361 |
| scheduler | `enabled=false` |
| SQLite SHA-256 | `3351796d7162b611c0f36924a707870a0e2024af3a62a498e006cde872d55745` |

## 2. 允许/阻断矩阵

| 操作 | 策略 | 本次结果 |
|---|---|---|
| W02–W06 | 允许旧 API 写入副本；W04/W06 保留旧 rebuild 路径 | W02–W06 均 200 |
| W08–W16 | 允许旧 API 写入副本 | W08/W09/W11/W12/W14/W15/W16 均 200；W10/W13 按旧语义返回 400/404 |
| W01 | `POST /api/article-covers` 永久阻断 | 409，`WRITE_REFERENCE_W01_BLOCKED` |
| W07 | `POST /api/aidso/keyword-heat` 永久阻断 | 409，未进入真实 Aidso |
| W17 | 单关键词刷新永久阻断 | 409 |
| W18/W19 | 批量取消/刷新永久阻断 | 409 |
| W20/W21 | scheduler config/trigger 永久阻断 | 409 |
| scheduler | 启动环境 `ZK_MONITOR_DISABLE_SCHEDULER=1`，副本 `scheduler.json.enabled=false` | PASS |
| 网络 | 仅允许 `127.0.0.1`、`::1`、`localhost` | 非 loopback 连接抛 `PermissionError`，有 guard 证据 |
| subprocess | `Popen/run/call/check_*`、`os.system/popen` 全部阻断 | 抛 `PermissionError`，有 guard 证据 |
| 外部 GET | 封面 GET、Aidso GET 也阻断，避免绕过 W01/W07 | 均 409 |

护栏事件写入 `evidence/guard-events.jsonl`，不写 freeze payload。

## 3. W02–W16 代表性回放

完整逐请求结果在 `evidence/replay.json`。共 22 个请求：

- 允许写操作 12 个返回 2xx：W02、W03、W04、W05、W06、W08、W09、W11、W12、W14、W15、W16；
- 合约负例 2 个返回 4xx：缺 `keyword` 的 W02 返回 400；商业价值越界 W15 返回 400；
- 永久阻断 6 个返回 409：W01、W07、W17、W19、W20、W21；
- W10 删除临时分组返回 400、W13 删除临时关键词返回 404，均为旧 API 实际回执，未伪装成功。

回放使用副本内真实关键词/分组 ID，并创建临时分组与临时关键词验证新增、改名、策略、商业价值、锁状态和落盘；没有真实抓取、Aidso、封面下载、调度触发或外网访问。

## 4. 写前后与重置证据

回放前后 `evidence/replay.json` 显示：

- 业务树 hash：发生变化；
- SQLite hash：发生变化；
- SQLite 行数变化可见：groups 18→19、registry 457→459（临时数据及状态写入）；
- `monitor-data.generated_at` 因旧 W04/W06 rebuild 更新，说明真实落盘路径被执行；
- W01/W07/W17/W19/W20/W21 没有落盘成功假象。

随后执行 `reset_write_reference.sh`。`evidence/reset.json` 已核对：

| 重置后检查 | 结果 |
|---|---|
| 业务树 hash 回到基线 | PASS |
| SQLite SHA-256 回到基线 | PASS |
| SQLite 完整性和表/行数回到基线 | PASS |
| JSON/兼容投影回到基线 | PASS |
| scheduler disabled | PASS |

## 5. 缺口与边界

1. W10/W13 的本次临时对象清理未得到 2xx：W10 400、W13 404；这是旧 API 实际行为，需后续按旧页面操作前置条件继续定位，不能改写为成功。
2. W04/W06 每次会执行旧 `rebuild_all`，本次各耗时约 2 分钟；这是旧 API 的真实副作用（仅副本内重建），因此回放客户端 timeout 设为 300 秒。
3. `start_write_reference.sh` 提供后台启动；本次最终验收使用前台启动以保留长任务进程和原始日志。未启动任何外网服务或真实抓取任务。
4. 该证据只证明旧 API 写副本的隔离与可重置，不代表 Hub 新 API 已完成逐字段影子比较或产品验收。
