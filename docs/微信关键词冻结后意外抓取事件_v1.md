# 微信关键词冻结后意外抓取事件 v1

> **事件级别**：P0
> **事件日期**：2026-07-16（Asia/Shanghai）
> **状态**：已止损；相关 launchd 已 bootout 且显式 disabled，待用户审阅
> **事件 ID**：`wechat-p0-freeze-tail-20260716`

## 1. 结论

2026-07-16 04:00:02 左右，旧真实源 `/Users/works14/.claude/监控/wechat-ybxhyyh-top3` 被 Codex 子进程意外启动；04:02:05 形成真实批次 `web_20260716_040205`，共 312 词。主代理约在 04:14 停止旧源 8765、runner `66432`、watchdog `66433`。

批次已成功完成 7 词；第 8 词“匠心飞越”第一次尝试因本地 Python `Bad file descriptor` 失败，第二次重试开始后被中止。旧源新增约 7 组搜索结果/正文，并修改 `normalized/accounts.json`、`normalized/articles.json`。第 8 组没有成功正文结果落盘。

> **`freeze_20260716T024524+0800` 仍是唯一有效迁移基线；`data/migration/wechat/reference/`（8774 reference）继续作为隔离参考。04:00:02 之后产生的 post-freeze tail 永不混入 v1 基线。旧源不删除、不回滚，保留为隔离事故证据。**

## 2. 证据审计

只读审计窗口为 `2026-07-16T04:00:02+08:00` 至 `2026-07-16T04:14:10+08:00`，按旧源文件实际 mtime、文件大小和 SHA-256 记录，共 70 个文件：

- 2 个冻结业务文件被修改：`normalized/accounts.json`、`normalized/articles.json`；
- 7 组搜索结果/正文输出（批次 item `001`–`007`）；
- 第 8 组仅产生运行日志、诊断和状态证据，未成功落地搜索结果/正文；
- 其余为批次启动、状态、事件、watchdog、重试和目录元数据文件。

逐文件清单、mtime、size、SHA-256：
`data/migration/wechat/incidents/p0-freeze-tail-20260716/legacy-source-tail-manifest.jsonl`

事件摘要及证据文件 hash：
`data/migration/wechat/incidents/p0-freeze-tail-20260716/incident.json`
`data/migration/wechat/incidents/p0-freeze-tail-20260716/manifest.json`

批次事实来自：

- `data/runs/web_20260716_040205/launch.json`
- `data/runs/web_20260716_040205/events.jsonl`
- `data/runs/web_20260716_040205/state.json`
- `data/runs/web_20260716_040205/diagnostics/008_匠心飞越.attempt1_041336.json`

## 3. 触发来源判断

**未知。** 现有进程快照、launchd 配置、`launch.json` 和批次日志能证明运行拓扑、启动参数及 `com.local.wechat-monitor-8765` 的 `RunAtLoad=true`/`KeepAlive=true` 配置，但没有足够证据证明最初由哪个调用者触发；本事件不猜测触发来源。

## 4. 基线与处置边界

1. post-freeze tail 永不并入 v1 基线、v1 导入、v1 对账或有效冻结快照。
2. 不删除旧源新增文件，不修改旧源，不用旧源回写或覆盖冻结 payload。
3. 不回滚 `normalized/accounts.json`/`normalized/articles.json`；它们的变化仅登记为事故影响。
4. 有效冻结目录及其 `MANIFEST.sha256`、8774 reference 均保持原样。
5. 后续若需研究 tail，只能在显式隔离批次中读取，并保留原始文件和 hash。

## 5. 重复安全检查

只读、无外网调用的检查脚本：

```bash
data/migration/wechat/safety/check_wechat_freeze_safety.sh
```

脚本检查 8765 listener、旧源 runner/watchdog/process、相关 launchd label 未加载且在 `launchctl print-disabled gui/$UID` 中明确为 `disabled`、有效 freeze ID、freeze manifest 及记录的 manifest verification；任一失败返回非 0。默认还会执行 freeze payload 的完整 SHA-256 manifest 校验。

本次完整校验仍会报告 3 个已知 `.DS_Store` hash 不一致：`payload/.DS_Store`、`payload/code-snapshot/.DS_Store`、`payload/code-snapshot/微信搜索结果/.DS_Store`。这些不一致已在 `data/migration/wechat/reference/evidence/freeze-manifest-verify-summary.json` 原始记录，并且冻结证据明确将 `.DS_Store` 排除在业务稳定哈希之外；脚本会**显式打印 WARN**，只在不存在其他文件 mismatch 时将业务 payload 检查判定为 PASS，不删除、重写或掩盖这 3 条不一致。

## 6. 当前状态

- 旧源 8765：已停止；
- runner `66432`：已停止；
- watchdog `66433`：已停止；
- `web_20260716_040205`：中止，最后持久化状态仍为 `running`，不视为成功；
- `com.local.wechat-monitor-8765`、`com.claude.schedule.wechat-ybxhyyh-top3`：均未加载且明确 disabled；
- 有效冻结与 8774 reference：继续有效，未被本次处置修改；
- 未启动旧源、未调用外网、未执行真实抓取。

## 7. 第二次安全事件：launchd KeepAlive 重生

### 7.1 时间线与止损

- `04:33:53`：`com.local.wechat-monitor-8765` 经 launchd 自动拉起旧真实源 `run.py`，PID `97967`、PPID `1`；
- 主代理 kill 后，`KeepAlive=true` 立即重生 PID `19441`；
- 约 `04:39`：主代理对 `com.local.wechat-monitor-8765`、`com.claude.schedule.wechat-ybxhyyh-top3` 执行 bootout 与 `launchctl disable`，并停止全部旧源进程；
- 审计时：8765 无监听、无旧源 runner/watchdog/run.py，两标签未加载且 `print-disabled` 均为 `disabled`。

第二次重生的**直接机制**可以确认是 launchd `KeepAlive`；但最初是谁、为何重新加载/启用该 label 仍无证据，**根触发来源继续记为 unknown，不猜测**。

### 7.2 04:33 后真实抓取检查

对以下旧源目录按 mtime 做只读检查，`04:33:00` 后新增/修改文件均为 0：

| 范围 | 04:33 后文件数 |
|---|---:|
| `normalized/**` | 0 |
| `data/runs/**` | 0 |
| `data/refresh_jobs/**` | 0 |
| `微信搜索结果/批量抓取/**` | 0 |

`/tmp/wechat-monitor-8765.log` 记录了页面/静态资源读取、refresh 状态/历史读取、bootstrap、关键词读取及 `POST /api/article-covers`，但未发现 refresh 启动请求、新 batch ID、runner/watchdog 或搜索结果落盘。结论是：**04:33 后未发现第二批真实搜索抓取或 normalized/run 新尾部。**

### 7.3 首次止损后、04:33 前的额外尾部

审计同时发现首次 manifest 截止时间 `04:14:10` 之后、第二次事件之前已有未登记的 post-freeze tail：`app/ingest/builders/monitor_scoring.py`、`data/state/app.db`、`normalized/snapshot_registry.json`、`snapshots.json`、`snapshot_terms.json`、`ranking_hits.json`、`monitor-data.json`、`accounts.json`、`articles.json` 等在 `04:24–04:32` 发生变化。其原因未知，且时间早于 `04:33:53`，不能归因于第二次 launch；这些内容同样**永久排除于 v1 基线**，不删除、不回滚。

另有 `app/static/js/monitor.js`、`app/static/css/monitor.css` 在 `04:33:44` 与 freeze hash 不同，时间早于第二次进程记录约 9 秒，原因未知，不归因、不回滚。

### 7.4 `.git` 元数据审计

旧源 `.git/FETCH_HEAD` 明确记录 remote `main` 的 fetch，objects/pack 出现对象材料，并存在 `refs/codex/turn-diffs/checkpoints/...` checkpoint ref。客观上可确认发生了 **Git fetch/object ingestion 与 Codex checkpoint 元数据活动**；但现有证据不能证明由哪个进程发起或为什么发生，原因记为 **unknown**。

本次只读审计还观察到部分 `.git` object/pack mtime 晚于最初报告的 `04:38`，因此不把 mtime 当作稳定归因证据；已在 `incident.json` 保存路径、SHA-256 样本和口径。未发现该 Git 活动在 `04:33` 后产生 normalized、run 或搜索输出；`.git` 变化原样保留，不清理、不回滚。
