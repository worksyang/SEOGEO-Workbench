# 公众号监控真实链路证据（2026-07-16）

## 结论

> **真实在线采集：BLOCKED。不得标记 PASS。** `127.0.0.1:28765` 可访问，账号、分类、历史任务和运行概览接口可读；但微信强校验返回 `logged_in=false`，同时原始扫码状态为已登录，运行时明确标记 `inconsistent=true`。按旧系统规则，未执行任何真实刷新/付费采集。

## 只读探测

| 项目 | 证据 |
|---|---|
| 探测时间 | `2026-07-15T17:25:37.730Z`（工作台记录） |
| 上游 | `http://127.0.0.1:28765` |
| `/health` | `200`，服务为 `mpgui-web` |
| `/api/accounts` | `200`，26 个账号 |
| `/api/categories` | `200`，2 个分类 |
| `/api/jobs` | `200`，可读到最近一次失败任务 |
| `/api/auth/wechat/check` | `200`，`logged_in=false`、`inconsistent=true` |
| `/api/runtime/overview` | `200`，`display_status=未登录`，提示“搜索公众号失败,请重新扫码授权！” |

## BLOCKED 任务持久证据

- Hub `mp_collection_jobs`：`mpj_01KXKD035SG3WTVCDPGQG4KNTD`，状态 `blocked`。
- `mp_collection_events`：同任务 `upstream_result/blocked`，错误类型 `login_inconsistent`。
- `audit_log`：`wechat-mp.job_create / blocked`，subject 为上述任务。
- `source_manifests`：`bcdb528f61b83e1d9c9dfa6c6fa7a4cfb6d19ab0af87efe398b0af678583cb3b`，类型 `live-http-evidence`，仅保存脱敏响应摘要和 endpoint。
- 任务错误：`WeRSS 登录状态不可用或与运行时不一致，任务未创建；请在旧控制台完成登录后重试。`

## 运行投影补验

为验证不依赖在线登录的历史 Markdown 投影，执行了 `limit=1` 的受控本地历史导入（不是在线刷新）：

- batch：`batch_51e11ed9d391fda0243c97b6`
- source manifest：`282168ac02724a34643097a8161bd0c9120287ac475122a16f5f2bc030e6b933`
- 结果：`partial_failed`，`accepted=866`、`rejected=380`；拒绝项保留在批次报告中。
- 运行层：文章通过 `mp_collection_events.event_type=article_observed` 关联到 `mp_collection_jobs`，任务结果和错误写入 checkpoint/command/audit。

## 代码与测试

- `features/mp/service.py`：在线探测证据 manifest、BLOCKED 任务落库、历史文章到运行事件投影。
- `services/mp_runtime.py`：文章事件投影；任务 checkpoint 同时保留 result/error。
- `tests/test_mp.py`：公众号运行任务、source manifest、审计错误、文章事件投影测试通过。
