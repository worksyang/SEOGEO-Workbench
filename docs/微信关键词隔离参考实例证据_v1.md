# 微信关键词隔离参考实例证据 v1

- **执行日期**：2026-07-16（Asia/Shanghai）
- **冻结基准**：`data/migration/wechat/freeze_20260716T024524+0800/payload`
- **恢复根**：`data/migration/wechat/reference/instance`
- **监听**：`127.0.0.1:8774`
- **旧真实源**：`/Users/works14/.claude/监控/wechat-ybxhyyh-top3`（本任务未读取为 cwd/数据根，未写入）
- **产品代码**：未修改 `workbench/`；未启动 `8765`；未 commit/push。

## 1. 可复现命令

```bash
cd /Users/works14/Documents/zkcode/260712_SEO-GEO
# 创建/重置 APFS clone 恢复根；若 cp -c 不支持，脚本会记录并回退普通 copy
 data/migration/wechat/reference/tools/prepare_reference.sh
# 停止当前实例
 data/migration/wechat/reference/tools/stop_reference.sh
# 删除恢复根、证据并从冻结 payload 重新建立
 data/migration/wechat/reference/tools/reset_reference.sh
# 启动（前台，可靠；另开终端保持该进程）
 data/migration/wechat/reference/tools/start_reference_foreground.sh
# 另开终端回放并生成证据
 python3 data/migration/wechat/reference/tools/replay_reference.py
```

本次实际恢复命令使用 `cp -cR` 成功，记录于 `data/migration/wechat/reference/evidence/prepare.json`，未发生普通 copy 回退。恢复布局由 `payload/code-snapshot` 建立，再覆盖冻结的 `payload/normalized`、`payload/data` 和 `payload/微信搜索结果`；未从旧真实源补数据。

## 2. 隔离与护栏结果

| 检查 | 结果 | 证据 |
|---|---|---|
| 8774 监听地址 | **PASS**：`Python PID 66426 TCP 127.0.0.1:8774 (LISTEN)` | `lsof`、`reference.stderr.log` |
| 8765 无监听 | **PASS** | 启动前及回放后 `lsof -nP -iTCP:8765 -sTCP:LISTEN` 无输出 |
| 旧 launchd 实例仍停 | **PASS**：`launchctl print gui/$(id -u)/...` 对两个旧 label 均 rc=113，未加载 | 启动检查输出；冻结报告记录 |
| 调度 | **PASS**：`ZK_MONITOR_DISABLE_SCHEDULER=1`，`/api/scheduler/status` 为 `enabled:false`、`base_url:http://127.0.0.1:8774` | `request-corpus.json` |
| 写入护栏 | **PASS**：POST/PATCH/DELETE 均返回可识别 409 `REFERENCE_WRITE_BLOCKED` | `request-corpus.json`、`guard-events.jsonl` |
| Aidso/封面外部 GET | **PASS**：均返回 409 `REFERENCE_EXTERNAL_BLOCKED`，未进入真实服务函数 | `request-corpus.json`、`guard-events.jsonl` |
| 进程网络/子进程 | **PASS**：非 loopback socket 与 subprocess 均抛 `PermissionError` | `process-guard-events.jsonl` |
| 恢复副本业务数据稳定 | **PASS**：前后均 `files=63464`、`bytes=1115485671`、SHA-256 `4c98da650e0b825f3e1c6c82e0f17facee3ecc46652c54dfaaeb4a39a2b56f4d` | `request-corpus.json` |

护栏只写入 `data/migration/wechat/reference/evidence/guard-events.jsonl`；不写冻结 payload。恢复副本的 `data/state/scheduler.json` 已在 clone 内显式设为 disabled，且 clone 内 scheduler 默认 base URL 改为 8774；冻结 payload 未改。

## 3. Frozen request corpus

`replay_reference.py` 固化并回放：

- **22 个 GET 路由**：覆盖旧 API 的全部 22 个 GET 声明；每条保存 method/path/query、状态码、`Content-Type`、`Content-Encoding`、`ETag`、`Cache-Control`、`Vary`、wire body SHA-256 和 size。
- **错误/缺失样本**：6 条，包括不存在 keyword/account/job、缺失 article 参数、路径穿越参数和非法文章列表参数。
- **R21**：7 种排序/筛选组合，均 `/api/articles`：`sort=reads`、`sort=likes`、`sort=rank`、`time_range=7`、`min_hits=2`、`account=<真实 account_id>`、`search=保险`。
- **写请求**：代表性 POST/PATCH/DELETE 各 1 条，状态均 409。

数量索引来自冻结 normalized：**312 keyword IDs、1361 account IDs、6364 article IDs**；文章正文路径字段共 **6364 条记录中可用路径数** 以 `request-corpus.json.identifiers.content_paths` 为准，具体路径不写入 docs，避免泄露大数据内容。完整证据留在 ignored `data/migration/wechat/reference/evidence/`。

核心结果：22 条 GET 中 18 条 200、2 条 404（冻结数据中真实缺失样本）、2 条 409（外部副作用阻断）；R21 7/7 为 200；写请求 3/3 为 409；业务 hash 前后一致。

## 4. 已知缺口

1. 非法 `/api/articles?page=0&page_size=9999&sort=not-real&time_range=bad` 按旧代码产生 500（`ValueError` 未转为 4xx）；这是旧实例真实行为，已保留，不用假 server 修饰。
2. `agent/evidence/missing-evidence`、`refresh-status/missing-job` 等缺失样本返回旧系统的 404，属于预期负例。
3. 当前证据是单机恢复副本回放，不代表已完成新 Hub 影子逐字段比较；该工作留给后续阶段。

## 5. 停止、重置与运行 PID

本次前台实例 PID 为 **66426**，端口 8774。停止可执行：

```bash
cd /Users/works14/Documents/zkcode/260712_SEO-GEO
 data/migration/wechat/reference/tools/stop_reference.sh
```

重置会停止实例、删除 `reference/instance` 与证据目录，再从冻结 payload 重新 clone：

```bash
 data/migration/wechat/reference/tools/reset_reference.sh
```

恢复副本可保持运行供主代理验收；禁止启动 8765，禁止改变冻结 payload，禁止把 `data/migration/wechat/reference/instance` 作为 Git 提交内容。

## 6. 冻结 payload 完整性核对

冻结报告将 `.DS_Store` 排除在业务稳定性之外。本次对冻结 manifest 的完整核对仍发现 3 个 `.DS_Store` 条目与 manifest 不符；这些条目属于冻结报告明确排除的元数据差异；本任务脚本没有写入 payload，也没有用它们作为服务数据，不能将该差异归因于本任务。排除冻结报告明确排除的 3 个 `.DS_Store` 后，其余 manifest 条目全部 OK；原始输出和摘要保存在 `data/migration/wechat/reference/evidence/freeze-manifest-verify*.txt/json`。这 3 个非业务元数据差异是本证据包的已知缺口，不应被误报为恢复副本业务数据变化。
