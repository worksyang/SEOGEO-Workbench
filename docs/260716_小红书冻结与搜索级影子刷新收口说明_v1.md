# 小红书冻结与搜索级影子刷新收口说明

## 当前边界

- 旧刷新、刷新状态、调度写入和设置写入均冻结；旧系统不会收到写请求。
- 影子刷新只处理 TikHub `search_notes` 的 `data.data.items`；不调用笔记详情、博主详情、封面、评论或媒体接口。
- 默认 Provider 为 `dry-run`。真实 Provider 只有同时满足 `HUB_XHS_SHADOW_PROVIDER_KIND=tikhub`、`confirm=true`、`dry_run=false` 且配置 `HUB_XHS_TIKHUB_TOKEN` 与 `HUB_XHS_TIKHUB_SEARCH_NOTES_URL` 才允许进入；Provider 使用 TikHub `search_notes` 的 GET + query 契约。
- token 只放在内存请求 Header，不写 SQLite、不写审计详情、不写日志。

## 已执行证据

- 2026-07-16 已完成 151 个关键词的搜索级全量影子刷新。
- 首轮 150 个成功、1 个 HTTP 400；使用新幂等键单独重试后最终 151/151 成功。
- 已写入原始响应、搜索快照、排名命中、去重后的内容、内容发现关系和搜索级互动观测。
- 已执行本地无上游修复，为已落库影子结果补齐作者身份、互动观测和发现关系；修复前已生成 SQLite 备份。
- 证据文件为 `data/migration/xhs/freeze_20260716T140000+0800/evidence/full_refresh_20260716_summary.json` 和同目录 JSONL 明细。

## 可验收操作方式（均不启动旧系统写任务）

1. **两词**：分别调用两个关键词的 `/api/v1/xhs/keywords/{keyword_id}/shadow-refresh`，默认 `dry_run=true`；使用不同 `Idempotency-Key`。
2. **十次**：对目标关键词循环 10 次，每次使用新的幂等键；重复幂等键会回放已有任务，不重复写入原始响应。
3. **全量**：读取 Hub 中 `platform='xiaohongshu'` 的关键词，逐词提交同一影子刷新接口；不得调用旧 `/api/keywords/*/refresh` 或旧 `/api/refresh-all`。

每次影子刷新会保存原始响应、响应哈希、搜索快照、排名命中、按 `note_id/规范化 URL` 去重后的笔记、内容发现关系、搜索级互动观测、任务运行记录和审计记录。搜索级全量调用已经执行；详情、博主详情、评论和媒体调用仍未执行。
