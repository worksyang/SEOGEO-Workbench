# 微信关键词冻结备份证据 v1

- 执行时间：2026-07-16（Asia/Shanghai）
- 旧源：`/Users/works14/.claude/监控/wechat-ybxhyyh-top3`（全程未写入；未调用 POST/PATCH/DELETE，未 cancel stale batch）
- **最终有效 freeze：`freeze_20260716T024524+0800`**
- 备份目录：`data/migration/wechat/freeze_20260716T024524+0800/`

## 1. 失败 freeze（保留，不作为迁移基线）

`freeze_20260716T022254+0800` 保留全部原始证据，明确判定 **FAIL / not a migration baseline**：

- 15 分钟观察 930 秒内发现 5 个旧源 `.DS_Store` 变化，raw observation 为 FAIL；
- `manifest-verify.json` 因将仍在写入的 `pre-freeze-evidence/observation-live.json` 纳入 manifest 而 FAIL；
- 未删除、未覆盖、未用其作为最终基线。

## 2. 最终有效 freeze 检查

| 检查 | 结果 |
|---|---|
| 两个相关 launchd 已停止 | PASS：`com.local.wechat-monitor-8765`、`com.claude.schedule.wechat-ybxhyyh-top3` 均不在 `launchctl list` |
| 8765 监听 | PASS：无监听 |
| 旧 runner/watchdog | PASS：按旧源绝对路径精确过滤为空；观察期间出现的 SSH `git-upload-pack` 是无关远程仓库进程，未停止 |
| `app.db` 占用 | PASS：`lsof` 为空 |
| 远程队列 | PASS：冻结前只读 GET `/api/refresh-all/status` 为 `idle`；历史列表中的 zombie batch 保留原状，未 cancel |
| 业务事实稳定观察 | PASS：16 个样本、1003 秒；normalized、app.db/scheduler、refresh_jobs、runs、Markdown、Agent、config、keyword_lists、backups 业务哈希零变化 |
| SQLite Online Backup | PASS：`integrity_check = ok`；5 张表行数为 352/392/46/18/457 |
| JSON | PASS：非空 JSON 全部可解析；空 orphan 单独登记，未删除 |
| 恢复演练 | PASS：隔离临时目录 SQLite/JSON/正文/快照/指标抽查通过 |
| 文件 manifest | PASS：139,585 条；`MANIFEST.sha256` 独立复核通过，无自引用 |

备份 payload：**139,583 个文件，2,236,835,520 bytes**。`file-manifest.jsonl` 的 SHA-256：`ac0133005b2a8155d3f4f80630cb8e09ef4d5f34f27ce9e27ae779165bf62af8`。

## 3. 明确排除项与恢复

业务稳定哈希排除并单独记录：`.DS_Store`、日志、冻结任务自行生成的 evidence 文件；不以排除项掩盖业务事实变化。敏感浏览器 profile/凭据未复制，仅记录存在与规模，未进入 Git、文档明文或 API。隔离参考实例**未启动**：需由专门 worker 在可写恢复副本且外部副作用禁用后执行。

最后事实时间：`monitor-data.generated_at=2026-07-15T21:33:51`；历史 zombie batch `web_20260715_203834` 的状态原样保留。

恢复 launchd（本次未执行）：

```bash
launchctl bootstrap gui/$(id -u) "$HOME/Library/LaunchAgents/com.local.wechat-monitor-8765.plist"
launchctl bootstrap gui/$(id -u) "$HOME/Library/LaunchAgents/com.claude.schedule.wechat-ybxhyyh-top3.plist"
```

未 commit、未 push。
