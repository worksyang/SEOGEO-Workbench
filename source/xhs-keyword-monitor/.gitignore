__pycache__/
*.pyc
*.pyo
*.pyd
.Python
.venv/
venv/
env/

# Secrets — NEVER commit real keys
.env

# Local state (SQLite, persisted UI state, refresh jobs)
data/state/
data/refresh_jobs/
data/runs/
data/tmp/
data/agent/
data/aidso_heat/

# Raw RedFox snapshots are large; keep out of git by default
raw/

# Local normalized caches — regenerated by rebuild
normalized/*.json

# Browser / playwright traces
.playwright-cli/
.playwright-mcp/

# IDE
.idea/
.vscode/
.DS_Store

# Logs
*.log
nohup.out
