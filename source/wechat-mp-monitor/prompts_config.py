# 标准模式提示词 - JSON格式输出
STANDARD_OCR_PROMPT = """请将图片内容转换为以下的json格式输出，输出的语言为简体中文，确保json中只有以下这些个字段，不允许增加别的字段，这是严格禁止的

### 处理规则：

1. **完整描述**：在"summary"字段：完整描述图片中的内容，类似于跟朋友讲述这张图的信息，朋友即便没看过这张图，经过你的描述，也能有深刻的印象。如有图中有表格，只需要说明图中哪个部分是表格就行，朋友能看到表格，但看不到表格以外的其他内容。
2. **文字判断标准**：
   - has_text为true的情况：图片中包含任何形式的可读文字（包括但不限于正文、表格文字、图表标签、标题等）
   - has_text为false的情况：图片完全没有任何文字内容（如纯风景照、抽象图案等）
3. **表格判断标准**：以下任意情况都应该将has_table设置为true：
   - 传统表格：有明显的行列结构，就是表格
   - 对比表：展示两个或多个对象的对比数据
   - 数据表：包含多行数据的列表式展示
   - 金融类表格：利率对比，产品收益对比等都是表格
     注意：即使表格被设计得很美观，没有明显的边框线，只要是按行列组织的数据，都应该视为表格
4. **二维码判断**：发现任何形式的二维码/小程序码都设为true
5. **表情包判断**：
   - 是表情包：具有夸张表情、搞笑效果的图片
   - 不是表情包：正常的照片、图表、截图等

### 输出格式规范：

```json
{
  "has_table": false, // 根据上述表格判断标准，注意：对比表、数据表、费率表等都属于表格
  "has_text": true,  // 只要图片中有任何文字就是true，包括表格中的文字
  "has_qrcode": false, // 包含二维码/小程序码为true
  "is_meme": false, // 是否为表情包
  "summary": "向朋友详细讲述这张图片，确保朋友可以通过你的描述，清楚理解图片的内容。图片中表格只需要告诉朋友即可，朋友已经看过表格了，不需要你再描述了"
}
```

请开始输出json，不要输出任何其他内容，你输出的json必须只有以上5个字段，不能自己随意增加字段，请牢记。对于has_text字段，记住：只要图片中包含任何形式的文字（包括表格中的文字、图表标注等）就应该设置为true。输出的summary的语言为简体中文，如遇到繁体中文，请翻译为简体中文，如有图中有表格，也就是"has_table"=true,表格部分的文字任何内容无需在这个summary中展示，因为我的其他代码会智能识别读取表格内容智能补充。
"""

# 简单模式提示词 - 纯文本描述
SIMPLE_OCR_PROMPT = """
角色：公众号配图描述师

500字以内，描述图片内容即可。

你收到的是一张公众号里面的文字配图，你的目标是描述这张图，让AI大模型在读取整个文章的时候，可以了解这张图的大致内容。

1.有信息量的图片 (包含文字、表格、图表等元素的任何图形化内容):500字以内，描述图片内容即可，主要是解释图片在文章中想要表达什么，500个字以内。
2.低信息量图像 (纯色块、无意义图案、装饰性箭头、表情包等):立即返回以下固定文本：该图片无任何信息，请删除。

请直接输出文字结果：
最终结果输出区域必须为```markdown\s*(.*?)\s*```的代码窗口类型输出，
因为最后我的代码会读取```markdown\s*(.*?)\s*```部分的内容
""" 

# 简单模式提示词 - 有水印账号特殊处理
SIMPLE_OCR_PROMPT1 = """
角色：图像分析引擎大师
你的任务是作为一个顶级的分析引擎，
将输入的任何图片精准地转换为结构完美的Markdown文本。

不同图像类型的处理方式：

1.照片:描述图片内容即可
2.结构化信息图 (包含文字、表格、图表等元素的任何图形化内容):
务必输出完整markdown格式的表格和其他文字内容。
由于有些表格转换为 md表格 的过程当中，我们要非常小心的去搞明白合并单元格的逻辑，以及要很仔细的去看清楚每一列和每一行合并单元格的情况，才能够不让单元格最终输出的时候出现有问题。

情况1：只有一张表
这种情况，由于表格头部合并单元格比较多，
1.请先确定整个表格有几列，通常是表格中间部分的合并单元格抢矿会少一些，列数准确
2.确定列数后，请确保你输出的每一行，列数都是一致的，这样才能保证下面的每一行的表格没有合并单元格的问题。
3.你要处理好表头的合并单元格，被合并的单元格用-表示或留空即可。（也就是如果有合并的单元格的话，请你拆分，大部分可是空白格子，第一个格子是正常的文本或数值即可，一般人都能理解这个是合并单元格。）

情况2：有几张表
依次输出几张表格即可，要求和上述要求一致

请完整的输出把图片转换为 Markdown，不要遗漏任何文字和或表格数据。
表格中的重点关键数据可以用emoji或者加粗来凸显一下，
使得表格可读性大大增强，简体中文（繁体中文或英文请转换为中文）完整输出整个结构化信息图的Markdown表格或文本。

3.文字型图片：直接输出图片上的文字即可，不要有任何思考过程。
4.低信息量图像 (纯色块、无意义图案、装饰性表情包等):立即返回以下固定文本，不得有任何增减：
该图片无任何信息，请删除。

你可以直接输出结果：
最终结果输出区域必须为```markdown\s*(.*?)\s*```的代码窗口类型输出，
因为最后我的代码会读取```markdown\s*(.*?)\s*```部分的内容

--thinking_budget: 8192
""" 

# 特殊账号配置 - 需要使用特殊OCR提示词的公众号列表
SPECIAL_OCR_ACCOUNTS = [
    "香港保险行业资讯","港险研究院","维港保典","长城港险","有态度的精蒜湿","Pippo的金融笔记","好险有胖虎","蓝星探险","港险优选专家",
    # 可以在这里添加更多需要特殊处理的公众号名称
]

# 复杂图片识别开关（用于表格复杂度判定；复杂表格同样只采用上方 AI 识别结果，不再做第三方 OCR）
ENABLE_COMPLEX_OCR = False  # True: 计算 is_complex 供日志/扩展；False: 始终视为非复杂

